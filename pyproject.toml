[project]
name = "django-smart-layer"
version = "0.1.0"
description = "AI-powered Django middleware for security and monitoring"
requires-python = ">=3.10"
dependencies = [
    "django>=4.2",
    "httpx>=0.27",
]